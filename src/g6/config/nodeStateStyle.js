export default {
  // 节点不同状态下的样式集合
  nodeStateStyles: {
    // 鼠标 hover 上节点，即 hover 状态为 true 时的样式
    hover: {
      stroke: 'black',
      fill: 'rgba(255,255,255,0)',
      cursor: 'pointer'
    }
    // 鼠标点击节点，即 click 状态为 true 时的样式
    // click: {
    //   stroke: '#000',
    //   lineWidth: 3
    // }
  }

}
